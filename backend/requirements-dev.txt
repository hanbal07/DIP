# Core dependencies needed for the main app (kept in sync with requirements.txt).
-r requirements.txt

# HTTP client for AI providers
httpx==0.28.1

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
asgi-lifespan==2.1.0
